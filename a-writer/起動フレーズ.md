# 実行手順(構成A)※人間向け

このフォルダをカレントに新規セッションを開き、`/model opus` にしてから:

```
/writer
```

工程の記録(日時・成果物・特記事項)はコマンドが 実行記録.md に自動追記する。
人間の仕事は、終了後に `/cost` の値を 実行記録.md に書き足すことだけ(トークン量はAIが自分で測れないため)。
コマンドの中身(=プロンプト)は `.claude/commands/writer.md`。変更したら変更履歴も記録する(規約の記録義務)。
